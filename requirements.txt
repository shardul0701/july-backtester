# requirements.txt

pandas
numpy<2.0
tqdm
boto3  # used for S3 report uploads only, not auth
requests
python-dotenv
PyYAML  # required for PIT ticker-history YAML parsing (helpers/point_in_time.py)
pandas-ta
orjson
pyarrow
yfinance  # required only when data_provider = "yahoo"
matplotlib  # candlestick charts and PDF plot generation
